## KeyTone v0.8 发布说明

全局键盘音效程序（幽灵版）：**开启后找不到关闭的地方**——无窗口、无控制台、无托盘图标、无退出快捷键。

### 功能亮点

- 三种模式 F12 循环切换：**按键音效**（每键绑定独立音效）/ **音符弹琴**（键盘即钢琴）/ **预制旋律**（内置乐谱音乐盒）
- **打断式播放**：新按键音立即替换旧音，快速连按不叠加不糊音
- 音量/音调实时调节：`=`/`-` 音量、`]`/`[` 音调
- 单实例保护、自定义轻量 toast 提示、免管理员权限（普通双击即用）

### 使用

1. 双击 `KeyTone.exe` 即启动（toast 提示「已启动」）
2. 停止：任务管理器结束 `KeyTone.exe` 进程（幽灵设计，无退出入口）
3. 内置音效：回车键为「牛来叫妈妈」前 5 秒，其余键为合成音效

### 技术栈

pynput 全局键盘钩子 + pygame 音频引擎 + tkinter 轻量 toast，PyInstaller 打包单文件（`--noconsole`），免 Python 环境。
