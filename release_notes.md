## KeyTone v0.11 发布说明

全局键盘音效程序（幽灵版）：**开启后找不到关闭的地方**——无窗口、无控制台、无托盘图标、无退出快捷键。

### 功能亮点

- **三种模式 F12 循环切换**：按键音效（每键绑定独立音效）/ 音符弹琴（键盘即钢琴）/ 预制旋律（音乐盒）
- **F11 多用途切换**：旋律切曲目（内置 6 首）/ 按键音效切方案（用户↔默认）/ 音符弹琴切音色（电子琴/正弦/方波）
- **toast 全面重构**：tkinter 极简圆角卡片——底部居中、Acrylic 磨砂半透明、堆叠动画（淡入/上推/淡出）、自动跟随系统深浅色与强调色、3 秒倒计时
- **调节键 F9/F10**：音量 -/+（功能键，不影响打字）
- **打断式播放**：新按键音立即替换旧音，快速连按不叠加
- **json 配置持久化**：按键-音效映射/音量/音调/旋律存 `config.json`，重启不丢失
- **预制音库**：`presets/` 内置 36 个电子琴音（C3~B5），可自由配置到任意按键
- 单实例保护、免管理员权限

### 使用

1. 双击 `KeyTone.exe` 即启动（toast 提示「已启动」）
2. 停止：任务管理器结束 `KeyTone.exe` 进程（幽灵设计，无退出入口）
3. 快捷键：F12 切模式 · F11 多用途 · F9/F10 音量
4. 配置：程序同目录 `config.json`（启动自动生成，可编辑按键映射/旋律/音量）

### 技术栈

pynput 全局键盘钩子 + pygame 音频引擎 + tkinter 自绘 toast（单 Tk 主线程架构），PyInstaller 打包单文件（`--noconsole`），免 Python 环境。
